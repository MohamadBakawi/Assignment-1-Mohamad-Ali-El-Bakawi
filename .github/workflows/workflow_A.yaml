name: Workflow A

on:
  workflow_dispatch:
    inputs:
      name:
        description: "Input Name:"
        required: true
        type: string

jobs:
  greeting:
    runs-on: ubuntu-latest
    steps:
      - name: Greeting Name
        run: echo "Hello, ${{ inputs.name }}!"